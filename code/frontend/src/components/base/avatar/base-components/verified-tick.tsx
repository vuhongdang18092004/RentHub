"use client";
import { AvatarSize } from "../avatar";

// TODO: Verified tick indicator overlay for user profiles.

export interface VerifiedTickProps {
  size?: AvatarSize;
  className?: string;
}

export function VerifiedTick(props: VerifiedTickProps) {
  return <div>TODO: VerifiedTick</div>;
}
