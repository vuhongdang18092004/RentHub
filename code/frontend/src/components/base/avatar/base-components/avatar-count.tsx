"use client";
import { AvatarSize } from "../avatar";

// TODO: AvatarCount displaying extra member counts in avatar groups (+N).

export interface AvatarCountProps {
  count: number;
  size?: AvatarSize;
  className?: string;
}

export function AvatarCount(props: AvatarCountProps) {
  return <div>TODO: AvatarCount</div>;
}
